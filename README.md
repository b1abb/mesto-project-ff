# 📸 Mesto

Учебный фронтенд‑проект: пользователи просматривают и добавляют карточки мест, ставят лайки, редактируют профиль и
аватар. Проект выполнен в парадигме модульного JavaScript в рамках курса **"JavaScript - 2"** от **Яндекс.Практикум**.

---

## 🚀 Демо / деплой

- GitHub Pages: **https://b1abb.github.io/mesto-project-ff/**

> Если вы форкнули репозиторий, включите GitHub Pages в настройках вашего форка (Deploy from `gh-pages` или из Actions —
> зависит от вашего пайплайна).

---

## 🧭 Основной функционал

- Загрузка профиля пользователя и карточек с сервера.
- Создание новой карточки (название + картинка).
- Просмотр изображения (карточка в модальном окне).
- Лайк/снятие лайка с карточки, отображение счётчика лайков.
- Удаление собственной карточки (с подтверждением).
- Редактирование данных профиля (имя, о себе).
- Обновление аватарки пользователя.
- Валидация всех форм (HTML5 + кастомные сообщения).
- Единая система модальных окон (открытие/закрытие по клику/ESC/оверлею).

---

## 🧩 Архитектура модулей

```
src/
├─ index.html                # Разметка, шаблон карточки, попапы
├─ pages/
│  └─ index.css              # Стили страницы
├─ scripts/
│  ├─ index.js               # Точка входа: инициализация, обработчики, взаимодействие модулей
│  ├─ api.js                 # Обёртка над fetch: авторизация, обработка ошибок, JSON
│  ├─ modal.js               # Управление попапами (open/close + обработчики событий)
│  ├─ card.js                # Создание/отрисовка карточки, like/remove
│  └─ validation.js          # Логика валидации, сообщения ошибок, состояния submit
├─ validationSettings.json   # Конфигурация валидации (селекторы/классы)
└─ accesses.json             # Конфигурация API (ENDPOINT, TOKEN, GROUP_ID)
```

Ключевые точки:

- **`index.js`** — описана инициализация приложения и основная логика страницы: поиск DOM-элементов на странице и
  навешивание на них обработчиков событий; обработчики отправки форм, функция-обработчик события открытия модального
  окна для редактирования профиля; функция открытия модального окна изображения карточки. Также в index.js находится
  код, который отвечает за загрузку карточек и данных пользователя при открытии страницы.
- **`api.js`** — универсальная функция `apiRequest(resourcePath, method?, body?)`, сборка URL из `accesses.json`,
  централизованная обработка ошибок (включая тело ответа).
- **`modal.js`** — описаны функции для работы с модальными окнами: функция открытия модального окна, функция закрытия
  модального окна, функция-обработчик события нажатия Esc и функция-обработчик события клика по оверлею.
- **`card.js`** — описаны функции для работы с карточками: функция создания карточки, функции-обработчики событий
  удаления и лайка карточки.
- **`validation.js`** — описаны функции для валидации форм. Из файла экспортируется только функция активации валидации
  `enableValidation` и функция очистки ошибок валидации `clearValidation`.

---

## 🔌 API и эндпоинты

Базовые методы (с учётом структуры запросов в проекте):

- **Профиль**
    - `GET  users/me` — получить данные пользователя
    - `PATCH users/me` — обновить имя/о себе `{ name, about }`
    - `PATCH users/me/avatar` — обновить аватар `{ avatar }`

- **Карточки**
    - `GET    cards` — получить список карточек
    - `POST   cards` — создать карточку `{ name, link }`
    - `DELETE cards/:id` — удалить свою карточку
    - `PUT    cards/likes/:id` — поставить лайк
    - `DELETE cards/likes/:id` — убрать лайк

---

## 🧾 Конфигурация доступа (`accesses.json`)

Проект читает конфиг напрямую в рантайме:

```json
{
  "ENDPOINT": "https://nomoreparties.co",
  "TOKEN": "ВАШ_API_ТОКЕН",
  "GROUP_ID": "cohort-XX"
}
```

- **`ENDPOINT`** — базовый URL сервера.
- **`TOKEN`** — токен авторизации.
- **`GROUP_ID`** — идентификатор учебной группы.

---

## ✅ Валидация

Используются HTML‑атрибуты (`required`, `minlength`, `maxlength`, `pattern`) + JS‑логика:

- Кастомные сообщения берутся из `data-error-*` атрибутов, например `data-error-pattern`.
- Ошибки рендерятся в `.<inputId>-input-error`.
- Кнопка сабмита блокируется до валидного состояния формы.
- Утилита `clearValidation(form, settings)` сбрасывает визуальные ошибки и состояние.

Пример атрибутов из формы:

```html
<input id="name"
       name="name"
       minlength="2"
       maxlength="40"
       required
       pattern="[A-Za-zЁА-Яа-яё \-]+"
       data-error-pattern="Разрешены только латинские, кириллические буквы, знаки дефиса и пробелы"/>
<span class="popup__form-input-error name-input-error"></span>
```

---

## 🧪 Обработка сетевых ошибок

Все запросы проходят через `apiRequest(...)`:

- Всегда пытаемся `response.json()`; при провале — `data = null`.
- При `!response.ok` выбрасываем `Error` cо статусом и расшифровкой причины из тела ответа.
- Локальный обработчик `handleResponseError` выводит ошибку в консоль; легко заменить на UI‑toast.

---

## 🧱 Быстрый старт

> Ниже — пример на Webpack. Актуальные команды смотрите в `package.json` вашего репозитория.

```bash
# 1) Клонирование
git clone https://github.com/b1abb/mesto-project-ff.git
cd mesto-project-ff

# 2) Установка зависимостей
npm install

# 3) Локальная разработка
npm run dev

# 4) Продакшен‑сборка
npm run build

# 5) Превью собранного билда (опционально)
npm run preview
```

---

## 🧱 Стандарт код‑стайла / соглашения

- Именование классов по **БЭМ**.
- ES6+, модульная структура.
- Неблокирующий UI, предсказуемая работа с состоянием.


